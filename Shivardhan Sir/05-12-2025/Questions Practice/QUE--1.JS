// Q1. Check given number is available  in range 100–200 or not. (only using ternary operator).

let num = 50;

let result = (num >= 100 && num <= 200) ? `${num} is in valid range` : `${num} is not in valid range`;

console.log(result);
