// Q10. Write a program to check whether a string starts with the letter A or a.
// Example:
// Input: "Apple" → true
// Input: "banana" → false

let input = "Apple";
let result = input.startsWith('a') || input.startsWith('A');
console.log(result);