// Q7. Check if a character is vowel or consonant

let char = 'h';
let charCapital = char.toUpperCase()

if(charCapital === 'A' || charCapital === 'E' || charCapital === 'O' 
    || charCapital === 'I' || charCapital === 'U') {
        console.log(`${char} is vowel`);
    }

else{
    console.log(`${char} is Constant`);
}